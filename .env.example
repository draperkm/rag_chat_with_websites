# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Pinecone Configuration
PINECONE_API_KEY=your_pinecone_api_key_here

# Optional: Set to True if you encounter KMP library conflicts
KMP_DUPLICATE_LIB_OK=True

# User Agent for web scraping (optional but recommended)
USER_AGENT=RAG-Chatbot/1.0 (Educational Project)

# Optional: Disable tokenizers parallelism to avoid forking warnings
TOKENIZERS_PARALLELISM=false
